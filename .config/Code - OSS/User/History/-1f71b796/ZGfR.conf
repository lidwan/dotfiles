general {
    lock_cmd = hyprlock     # what to run when loginctl or anything calls to lock
}

listener {
    timeout = 500 # in seconds
    on-timeout = loginctl lock-session
    on-resume = notify-send "Welcome back!"
}
